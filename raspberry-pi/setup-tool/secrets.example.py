# Template for secrets.py — copy to secrets.py and fill in real values.
# secrets.py is gitignored and must never be committed.

PI_USER      = "museumgh"
PI_PASSWORD  = ""           # SSH password for the Pi user

SANITY_TOKEN = ""           # Sanity API write token (from sanity.io → API → Tokens)

DEFAULT_WIFI = [
    # {"ssid": "YourNetwork", "password": "yourpassword", "priority": 10, "description": "museum"},
]

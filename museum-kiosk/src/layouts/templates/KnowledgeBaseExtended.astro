---
import '../../styles/kiosk-clean.css';

const { config } = Astro.props;
const articles = config.content?.articles || [];
---

<div class="kiosk-page">
  <div class="kiosk-header">
    <div class="container">
      <h1 class="clean-section-title">📚 Erweiterte Wissensdatenbank</h1>
    </div>
  </div>
  
  <div class="kiosk-content">
    <div class="container">
      {articles.length > 0 ? (
        <div class="clean-grid clean-grid-2">
          {articles.map((article: any, index: number) => (
            <div class="clean-card">
              <h3>{article.title || `Artikel ${index + 1}`}</h3>
              <p class="text-muted">{article.description || 'Beschreibung folgt...'}</p>
              <div class="clean-btn-group">
                <button class="clean-btn clean-btn-primary">Lesen</button>
                <button class="clean-btn">Details</button>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div class="clean-empty">
          <div class="clean-empty-icon">📖</div>
          <p class="clean-empty-text">Keine Artikel verfügbar</p>
          <button class="clean-btn clean-btn-primary">Artikel hinzufügen</button>
        </div>
      )}
    </div>
  </div>
</div>

<style>
  .kiosk-page {
    width: 100vw;
    min-height: 100vh;
    background: var(--color-bg);
    color: var(--color-primary);
  }
</style>